# log-analyzer
This project is a Python-based log analysis tool for Linux authentication logs, focused on SSH security events. It detects brute-force activity, extracts suspicious IP addresses, and outputs JSON security reports to simulate SIEM-style alerting and SOC workflows.
